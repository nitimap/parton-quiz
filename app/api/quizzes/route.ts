import { NextResponse } from "next/server";import { isParent } from "@/lib/auth";import { adminClient } from "@/lib/supabase/server";import { z } from "zod";
const schema=z.object({title:z.string().trim().min(1).max(120),subject:z.string().trim().max(80).optional(),description:z.string().trim().max(500).optional(),sourceFilename:z.string().max(255).optional(),questions:z.array(z.object({id:z.string(),originalNumber:z.number(),question:z.string().min(1),choices:z.array(z.object({id:z.string(),text:z.string().min(1),originalLabel:z.string().optional()})).length(4),correctChoiceId:z.string().min(1)})).min(1)});
export async function GET(){if(!await isParent())return NextResponse.json({error:"Unauthorized"},{status:401});const db=adminClient();if(!db)return NextResponse.json({error:"Supabase is not configured."},{status:503});const {data,error}=await db.from("quizzes").select("*").order("created_at",{ascending:false});return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json(data)}
export async function POST(req:Request){if(!await isParent())return NextResponse.json({error:"Unauthorized"},{status:401});const parsed=schema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:"Quiz data is invalid."},{status:400});const db=adminClient();if(!db)return NextResponse.json({error:"Supabase is not configured."},{status:503});const q=parsed.data;const {data,error}=await db.from("quizzes").insert({title:q.title,subject:q.subject||null,description:q.description||null,source_filename:q.sourceFilename||null,questions:q.questions}).select().single();return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json(data,{status:201})}
